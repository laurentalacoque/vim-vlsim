Things to do for compatibility:

- 7.x doesn't support default values for arguments
- 7.x doesn't support function('funcname',[arglist]), must convert to function('funcname') somehow

- test with /bin/gvim (7.4)
